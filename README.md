# AURA - Анонимный веб-мессенджер

Современный анонимный мессенджер с акцентом на приватность и минималистичный дизайн. Никаких email, телефонов или социальных сетей — только ник и пароль.

## 🚀 Демо-версия (одностраничное приложение)

Для быстрого ознакомления с интерфейсом и функциональностью доступна **полностью рабочая демо-версия в одном HTML-файле**:

1. **Скачайте** файл `index.html` из этого репозитория
2. **Откройте** его в любом современном браузере (Chrome, Firefox, Safari, Edge)
3. **Используйте** любые данные для регистрации (демо-версия работает полностью в браузере)

**Особенности демо-версии:**
- Полностью рабочий интерфейс мессенджера
- Имитация общения в реальном времени
- Тёмная/светлая темы
- "Загрузка" файлов и голосовых сообщений
- Уведомления в браузере
- Все данные хранятся только в памяти браузера (исчезают после обновления)

**Подробная документация по демо-версии:** [README-AURA.html](README-AURA.html) (откройте в браузере)

---

## 📦 Полная версия (backend + frontend)

Ниже описана полная версия проекта с backend на Node.js, frontend на React и базой данных PostgreSQL.

## Особенности

- 🔒 **Анонимная регистрация** - только ник и пароль
- 🎨 **Современный UI** - вдохновлён дизайном Яндекса и Альфа-Банка
- 💬 **Реальное время** - WebSocket для мгновенных сообщений
- 🎤 **Голосовые сообщения** - запись и отправка прямо в браузере
- 📁 **Медиафайлы** - загрузка изображений, видео и файлов
- 🌙 **Тёмная/светлая тема** - с автоматическим определением
- 🔔 **Уведомления** - браузерные и в приложении
- 👥 **Личные и групповые чаты** - создание, поиск, управление
- ⚡ **Быстрая работа** - оптимизированная архитектура

## Технологический стек

### Backend
- **Node.js** + **TypeScript** - серверная платформа
- **Express** - веб-фреймворк
- **Socket.IO** - WebSocket для реального времени
- **PostgreSQL** - реляционная база данных
- **TypeORM** - ORM для работы с БД
- **bcrypt** - хеширование паролей
- **JWT** - аутентификация
- **Multer** - загрузка файлов

### Frontend
- **React** + **TypeScript** - пользовательский интерфейс
- **Vite** - сборка и разработка
- **Tailwind CSS** - стилизация
- **Socket.IO Client** - WebSocket клиент
- **React Query** - управление состоянием
- **Framer Motion** - анимации
- **React Router** - навигация

## Установка и запуск

### Быстрый старт (демо-версия)
1. Скачайте файл `index.html`
2. Откройте его в браузере двойным кликом
3. Или запустите локальный сервер:
   ```bash
   # С Node.js
   npx http-server -p 8080
   
   # С Python
   python -m http.server 8080
   ```
4. Откройте `http://localhost:8080` в браузере

### Полная версия (требует настройки)

#### Предварительные требования
- Node.js 18+ 
- PostgreSQL 14+
- npm или yarn

#### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd aura-messenger
```

#### 2. Настройка базы данных
```bash
# Создайте базу данных в PostgreSQL
createdb aura_messenger

# Или используйте psql
psql -c "CREATE DATABASE aura_messenger;"
```

#### 3. Настройка переменных окружения
Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
# Порт сервера
PORT=3001

# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=aura_messenger
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Загрузка файлов
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=20971520 # 20MB в байтах

# CORS
FRONTEND_URL=http://localhost:5173

# WebSocket
WS_PATH=/socket.io
```

#### 4. Установка зависимостей
```bash
# Установить зависимости для backend и frontend
npm run install:all
```

#### 5. Запуск миграций базы данных
```bash
# Создание таблиц в БД
npm run db:migrate
```

### 6. Запуск в режиме разработки
```bash
# Запустить backend и frontend одновременно
npm run dev
```

Или отдельно:
```bash
# Backend
npm run dev:backend

# Frontend (в новом терминале)
npm run dev:frontend
```

### 7. Открыть приложение
Откройте браузер и перейдите по адресу:
```
http://localhost:5173
```

## Структура проекта

```
aura-messenger/
├── backend/                    # Серверная часть
│   ├── src/
│   │   ├── config/           # Конфигурация
│   │   ├── controllers/      # Контроллеры API
│   │   ├── entities/         # Сущности БД (TypeORM)
│   │   ├── middlewares/      # Промежуточное ПО
│   │   ├── routes/           # Маршруты API
│   │   ├── services/         # Бизнес-логика
│   │   ├── sockets/          # Обработчики WebSocket
│   │   ├── utils/            # Вспомогательные функции
│   │   └── server.ts         # Точка входа сервера
│   ├── migrations/           # Миграции БД
│   └── uploads/              # Загруженные файлы
├── frontend/                  # Клиентская часть
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   ├── contexts/         # React контексты
│   │   ├── hooks/            # Кастомные хуки
│   │   ├── pages/            # Страницы приложения
│   │   ├── services/         # API сервисы
│   │   ├── store/            # Состояние (React Query)
│   │   ├── styles/           # Глобальные стили
│   │   ├── types/            # TypeScript типы
│   │   ├── utils/            # Вспомогательные функции
│   │   └── main.tsx         # Точка входа приложения
│   ├── public/               # Статические файлы
│   └── index.html            # HTML шаблон
├── package.json              # Корневой package.json
└── README.md                 # Этот файл
```

## API Эндпоинты

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

### Пользователи
- `GET /api/users/search?nickname=` - Поиск по нику
- `PUT /api/users/profile` - Обновление профиля
- `PUT /api/users/settings` - Обновление настроек

### Чаты
- `GET /api/chats` - Список чатов пользователя
- `POST /api/chats` - Создание чата
- `GET /api/chats/:id` - Информация о чате
- `PUT /api/chats/:id` - Обновление настроек чата
- `GET /api/chats/:id/messages` - Сообщения чата
- `POST /api/chats/:id/messages` - Отправка сообщения
- `PUT /api/chats/:id/messages/:messageId` - Редактирование сообщения
- `DELETE /api/chats/:id/messages/:messageId` - Удаление сообщения

### Файлы
- `POST /api/upload` - Загрузка файла
- `GET /uploads/:filename` - Получение файла

## WebSocket события

### Клиент -> Сервер
- `join` - Присоединиться к комнатам чатов
- `message:send` - Отправить сообщение
- `message:typing` - Индикатор набора текста
- `message:read` - Отметить сообщение как прочитанное

### Сервер -> Клиент
- `message:new` - Новое сообщение
- `message:edited` - Сообщение отредактировано
- `message:deleted` - Сообщение удалено
- `user:online` - Пользователь онлайн
- `user:offline` - Пользователь оффлайн
- `user:typing` - Пользователь печатает

## Разработка

### Создание миграции
```bash
npm run db:generate -- -n MigrationName
npm run db:migrate
```

### Сборка для продакшена
```bash
# Собрать frontend
npm run build:frontend

# Собрать backend
npm run build:backend

# Запустить в продакшене
npm run start
```

### Линтинг и форматирование
```bash
# Проверка типов TypeScript
npm run type-check

# Линтинг
npm run lint

# Форматирование
npm run format
```

## Безопасность

- Пароли хешируются с использованием bcrypt
- JWT токены хранятся в httpOnly cookies
- CORS настроен только для разрешённых доменов
- Rate limiting на эндпоинты аутентификации
- Загрузка файлов с проверкой типов и размера
- SQL-инъекции предотвращаются TypeORM

## Лицензия

MIT
